{
  "name": "my-first-pages",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build-site",  // Add a build script for your static site
    "deploy": "wrangler pages publish ./dist",  // Assuming ./dist is your build folder
    "dev": "wrangler dev",  // Keep the dev server for local development
    "test": "vitest"  // Keep testing script if needed
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.2",  // Keep if you need testing in Pages
    "wrangler": "^3.95.0",  // Wrangler is still needed for deployment
    "vitest": "2.1.8",  // Keep testing library
    "your-static-site-builder": "^1.0.0"  // Replace this with your static site generator, e.g., React, Vue, etc.
  }
}
