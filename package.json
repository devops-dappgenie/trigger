{
  "name": "my-first-pages",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build-site",  
    "deploy": "wrangler pages publish ./dist",  
    "dev": "wrangler dev",  
    "test": "vitest"  //
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.2", 
    "wrangler": "^3.95.0", 
    "vitest": "2.1.8",  
    "your-static-site-builder": "^1.0.0"  
  }
}
